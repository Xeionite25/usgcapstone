declare enum EVENTS {
    LOAD = "load",
    VIEWPORT = "viewport",
    ATTRIBUTE = "attribute",
    CHILDREN = "children",
    DATA = "data"
}
type UseEvent = {
    event: EVENTS;
    target: string | HTMLElement;
    root: null | string | HTMLElement;
    effect: unknown;
};
declare const useEvent: ({ event, root, target, effect }: UseEvent) => void;
export { useEvent, EVENTS };
